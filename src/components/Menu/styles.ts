//import styled from "styled-components";

import { CSSProperties } from "styled-components";

export const menuModal:CSSProperties = {
    minHeight: '100vh',
    top: '0',
    position: 'absolute'

}
export const menuAntd:CSSProperties = {
}